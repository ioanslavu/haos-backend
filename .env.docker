# Django
DJANGO_SECRET_KEY=replace-this-in-prod
DEBUG=False

# Hosts
ALLOWED_HOSTS=haos.dev,haos.baby,api.haos.dev,api.haos.baby,localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://haos.dev,http://haos.baby,https://haos.dev,https://haos.baby,http://localhost,http://127.0.0.1,https://localhost
CORS_ALLOWED_ORIGINS=http://haos.dev,http://haos.baby,https://haos.dev,https://haos.baby,http://localhost:8080

# Database (Amazon RDS)
# Set these to your RDS values
DB_NAME=haos
DB_USER=haos
DB_PASSWORD=change-me
DB_HOST=your-rds-endpoint.rds.amazonaws.com
DB_PORT=5432

# Celery / Redis
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# OAuth redirect target (frontend URL)
OAUTH_FRONTEND_URL=https://haos.dev

# Crypto (rotate in production)
FIELD_ENCRYPTION_KEY=
